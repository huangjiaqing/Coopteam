import React, { Component } from 'react';
import styles from './ProjectSet.css';

export default class ProjectSet extends Component {

  render() {

    return (
      <div className={styles.projectSet}>
      </div>
    );
  }
}